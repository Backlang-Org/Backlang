StreamPlan Backseat-Montag:
	
	- Scopes implementieren

	- macro folgende syntax erlauben:
		mymacro(args) {
			//do something
		}

	- bitwise operators
	& | ^ ~
	- bitwise assignment operators
	&= |= ^= ~=

	- char-literal und type
		'' im lexer nicht mehr als string nutzen, sondern als char
		char als primitiven datentyp implementieren in syntax highlighting

	- macro-function-calls with #