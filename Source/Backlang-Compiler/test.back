register EAX = 0;

type int = i32;
const bits = 32;

global flags : Flags = #EAX;
global behavior : Flags = &250;

fn main() -> i32 {
	let something : int* = 12;
	doSomething();
	0;
}

fn doSomething() {
	print("Hello World");
	declare arr = [12, 42, 3];

	print(arr[0]);

	asm {
		mov 12, EAX;
		something {
			xor EAX, EAX, EAX;
		}

		move 42, &[0xFF + 4];
		move &[0xFF + 4], &[PTR 0x4];
		jmp $something;
	}

	/*
	multi
	line
	comment
	*/

	while true { 
		doSomething(2);	
	}

	for x : i32 in 1..12 {
		print(x);
	}

	for x in arr {
		print(x);
	}

	print(#EAX); // load value from register EAX (0) as argument
}

enum Colors {
	Red = 0xFF0000,
	Green = 0x00FF00,
	Blue = 0x0000FF
}

bitfield Flags {
	DivideByZero = 0,
	IsZero = 1,
}

enum Instructions {
	Invalid,
	Move = 1 << 12,
	Pop,
}

fn tests() {

	let kkk = sizeof<Colors>;
	let mm : i32 = default;
	let mn = default<i32>;

	let a = 12;
	let mut b : i32 = 42;
	let c = &a;

	let d : list<i32> = [];

	let e = list<list<i32>> = [];

	a <-> b;

	a = b % 3;

	if a <= 12 and a >= 13 {

	}
	else {
	
	}

	let result = match a with
		12 => 13,
		> 13 => 15,
		_ => 0;

	none;

}