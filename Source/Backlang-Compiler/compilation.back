
func main() static -> none {
	let uh = 1L;

	let msg : string = concatId(a, b, c);
	let name : string = nameof(msg);
	let k = 1.2;

	let s = Point::new();
	let handle = ^s;
	let another = ^s;
	#autofree(handle, another) {
		print(&handle);
	}

	let myChar = 'a';

	

}
