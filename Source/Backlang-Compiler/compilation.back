using Console::WriteLine as println;

func main() static -> none {
	let uh = 1uL;
	let cool = 3f;
	
	let msg : string = concatId(a, b, c);
	let name : string = nameof(msg);
	let k = 1.2;

	let myFraction = 1 \\ 2;

	let s = Point::new();
	let handle = ^s;
	let another = ^s;
	#autofree(handle, another) {
		println(&handle);
	}

	let myChar = 'a';

	

}
