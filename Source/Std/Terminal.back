import Memory;

struct Terminal {
	End = Width * Height;
	Width = 80; // u32 is standard if no type set
	Height = 25;

	CursorX = 0;
	CursorY = 0;
}

implementation of static Terminal {
	fn write(char : i8) {
		let address : u32* = CursorY * Width + CursorX;

		*address = char;

		if CursorX = Width  {
			CursorX = 0; 
			CursorY++;
		}
	}

	fn write(str : string) {
		for c in str) {
			write(c);
		}
	}

	fn readChar() {
		
	}

	fn clear() {
		Memory::clear(0, End);
	}
}